[package]
name = "claude-usage-widget"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "claude-usage"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["now"] }
